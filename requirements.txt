# Application dependencies for transcript-qwen-asr.
#
# torch is intentionally NOT pinned to a CUDA wheel here, because the right
# wheel depends on your installed NVIDIA driver / CUDA runtime. Install torch
# FIRST, choosing the matching wheel index from https://pytorch.org/get-started/locally/
#
# Recommended for NVIDIA Blackwell (RTX 50-series, B100/B200 — sm_100/sm_120):
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch
#
# Other valid options:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch  # also supports Blackwell, more battle-tested
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch  # Hopper/Ada and older — NOT Blackwell
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch  # Hopper/Ada and older — NOT Blackwell
#
# Then:
#   pip install -r requirements.txt

qwen-asr>=0.0.6
torch>=2.11
numpy>=1.26
tqdm>=4.66
rich>=13.7
